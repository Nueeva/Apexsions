# ==========================================================
#              APEXSIONS ECONOMY CONFIGURATION
# ==========================================================

database:
  file: "economy.db"

currencies:
  rupiah:
    name: "Rupiah"
    symbol: "Rp."
    starting-balance: 1000.0
    transferable: true
  diamond:
    name: "Diamond"
    symbol: "Diamond"
    starting-balance: 0.0
    transferable: true

auction:
  default-duration-hours: 24
  cleanup-interval-seconds: 30
